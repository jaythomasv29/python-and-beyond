# Set = an unordered & unique list

my_set = {1, 2, 3, 4, 5}
other_set = {4, 5, 6, 7, 8, 9}

my_set.add(10)
print(my_set)


# Set Methods
# my_set.difference(other_set) # returns a set of the difference between two sets
# my_set.add(10) # add to set in place
# my_set.intersection(other_set) # return a set of values that are in common between two sets
# my_set.isdisjoint(other_set) # returns a bool to ccheck if two sets have no similar values
# my_set.union(other_set) # combine two sets together and remove duplicates